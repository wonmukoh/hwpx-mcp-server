# 판올림 기록

## 다음 판 (아직 안 내놓음)

처음 쓰는 사람처럼 **깐 꾸러미를 훑다가** 나온 것들이다.

### 새로 되는 것

- **`edit` 에 `delete_row`.** `insert_row` 는 있는데 **지우는 짝이 없었다.**
  양식에는 넉넉히 만들어 둔 줄이 있다 — 예산 표에 다섯 줄인데 쓸 것이 셋이면
  둘이 남는다. 넣을 수만 있고 뺄 수는 없으니 빈 줄을 그대로 두는 수밖에 없었다.

  지우는 것은 되돌릴 수 없어 **셋을 막는다** — 마지막 줄, 합친 칸을 반만 지우는 것,
  **글이 든 줄**. 셋째는 `force: true` 로 넘기되 지운 글은 못 되돌린다.
  `at` 은 **꼭 줘야 한다** — `insert_row` 처럼 기본값을 두면 엉뚱한 줄이 말없이 날아간다.

- **`--help` 와 `--version`.** 처음 쓰는 사람이 `npx hwpx-mcp` 다음으로 가장 먼저
  치는 것이 `--help` 인데, 인자를 통째로 무시하고 stdio 서버를 띄워
  **멈춰 선 것처럼** 보였다.

### 고친 것

- **XML 이 못 쓰는 글자를 그냥 받았다.** `U+0000` 같은 C0 제어문자는 XML 1.0 에서
  **어떤 방법으로도 못 쓴다** — 숫자 참조로도 안 된다. 그런데 넣으면 **저장까지 됐고**,
  그 파일을 한글에 물으니 `OPENFAIL` 이었다. 저장이 됐다고 쓸 수 있는 게 아니다.

  이제 쓰는 길목에서 **몇 번째 글자인지 짚어 주고** 막는다. 말없이 빼지 않는다 —
  빼면 글이 조용히 달라진다. 저장 길목에도 그물을 걸어 딴 길로 새어 들어와도 잡는다.
  줄바꿈·탭은 규격이 허락하는 것이라 그대로 통과한다.

  **한 길만 막았더니 딴 길로 샜다.** 처음엔 `set_text` 만 막았는데 `replace` 로는
  그냥 들어갔다. 글이 들어오는 길 여덟을 하나씩 찔러 보고 다 막았다 —
  `set_text`(문단·칸) · `replace` · `compose`(본문·표 칸·제목·개조식·머리말).
  `compose` 는 **어느 블록의 어느 자리**인지까지 짚는다:
  `blocks[2].rows[1][1] 의 1번째 글자 U+0000`.

- **합친 칸이 안 보였다.** 덮인 자리가 딴 칸처럼 나와서 —

      표 전체:  0,0="여기"  1,0="여기"      ← 한 칸인데 두 번 나온다
      cell_x_1_0 에 쓰면 → "이미 같은 글이라 바뀐 것이 없다"

  양식을 채우는 모델이 이걸 보면 **두 칸인 줄 알고** 서로 다른 걸 쓰려다 막힌다.
  그리고 그 문구로는 왜 막혔는지 알 길이 없다.

  이제 `get_content(표)` 가 `rowspan`·`colspan`·`covered_by` 를 낸다.
  덮인 자리에 쓰려 하면 **어느 칸을 쓰라고 짚어 준다.**
  `find(kind:"cell")` 도 덮인 자리는 안 낸다.

- **굽기가 겹쳐 돌면 `dist` 가 깨졌다.** 검증 한 바퀴가 여섯 번 굽고 vitest 는
  시험 파일을 나란히 돌린다. 자리 이름이 같아 서로의 굽는 폴더를 지웠고,
  둘이 동시에 옮기다 `EPERM` 이 났고, **바꾼 뒤에 `dist` 를 열어 확인하느라**
  손잡이를 쥔 채라 남이 이름을 못 바꿨다.
  이제 자리에 pid 를 붙이고, 확인을 **자리 바꾸기 앞**에서 하고, 바꾸는 순간만
  줄 세운다. 넷을 동시에 돌려도 오류가 없다.

---

## 0.4.1 — 2026-08-28

### 고친 것

- **서버가 제 판을 잘못 말했다.** `package.json` 은 0.4.0 인데 클라이언트에게는
  `0.1.0` 이라고 답했다 — 판이 두 군데 따로 적혀 있었다.
  이제 `package.json` 에서 읽는다. 갈라지면 시험이 걸린다.
- **`repository` 를 적었다.** 없어서 npm 페이지의 문서 링크가 다 죽어 있었다.
  파일은 꾸러미에 담겨 있었지만, npm 웹은 상대 링크를 저장소 기준으로 푼다.
- `compose` · `create_document` 설명에 **「채울 양식이 이미 열려 있으면 쓰지 마라」** 를 넣었다.
  한 엔진이 열어 둔 양식을 놔두고 새 문서를 지어 딴 폴더에 저장한 일이 있었다.

### 재는 것을 늘렸다

- **규격판 여섯 가지**로 두드려 본다 — SDK 가 받는 다섯에 **모르는 판** 하나를 더해서.
  모르는 판을 보내도 거절하지 않고 아는 최신 판으로 답하는지 잰다.
  클라이언트 이름을 세는 대신 규격으로 잰다.
- **헛도는 시험을 찾는 갈래.** 앞에 `expect` 가 없는 건너뛰기, 이 기계에만 있는 절대 경로,
  `expect` 가 하나도 없는 `it` 을 잡는다. 만들자마자 둘을 잡았다.
- **내놓을 채비 갈래.** README 가 가리키는 문서가 꾸러미에 담기나, 라이선스가 있나,
  `bin` 이 죽지 않나. `prepublishOnly` 에도 박혀 있다.

### 그 밖에

- 안티그래비티 설치 안내 (`~/.gemini/config/mcp_config.json`)
- 시험 자료에서 실제 학교 이름을 빼고 지어낸 이름으로 갈았다

---

## 0.4.0 — 2026-08-27 · 첫 배포

한글 문서(HWPX)를 **한글 없이** 읽고 쓰는 MCP 서버. 도구 열한 개.

이 판부터 **실제로 쓰인다.** 학교 문서 작성 앱이 이걸 물고 돌고,
가정통신문·계획서를 도구만으로 끝까지 만든다.

### 도구

| | |
|---|---|
| 문서 | `create_document` `open_document` `save_document` `close_document` `list_documents` |
| 읽기 | `get_outline` `get_content` `find` `get_styles` |
| 쓰기 | `compose`(맨바닥에서 짠다) `edit`(있는 것을 고친다) |

`edit` 은 `op` 로 가른다 — `set_text` · `replace` · `set_style` · `insert_row` · `insert_image`.

### 왜 열한 개인가

옛 판은 125개였다. 서버에 물어 센 수다.

| | 옛 판 | 0.4.0 |
|---|---|---|
| 도구 | 125개 | **11개** |
| `tools/list` 답 | 71,926자 | **18,211자** |

`tools/list` 는 **매 요청마다** 붙는다. 이 차이가 거기서 값을 한다.

### 이 판에서 고친 것

**읽기**

- `get_content(doc_id)` — `id` 없이 부르면 **문서 전체 글**. 고치기 전에 흐름을 읽는 길.
- `get_content(id:"tbl_…")` — 칸마다 `{id, text, row, col}` 와 `empty`.
  「셋째 줄」 같은 **위치 지시를 ID 로 옮길** 수 있다.
- `get_content(id:"cell_…")` — 칸 글을 **줄바꿈으로 갈라** 주고 `paragraphs` 로 문단 ID 까지.
- `find(kind:"paragraph")` — **문서 차례대로**, 빈 문단까지, 기본 500개.
  빈 자리는 글이 없어 `text` 로 못 찾는다. 이 길이 유일하다.
- `find` 의 문단마다 **`in_cell`** — 셀 안의 표까지 내려가 **가장 안쪽 칸**을 가리킨다.
- `find(kind:"table")` 에 `rows` · `cols` · **`empty`** — 빈 표를 가릴 수 있다.
- `find` · `get_outline` 이 **`truncated`** 를 낸다. 잘렸으면 그렇다고 말한다.

**쓰기**

- `set_text(id:"cell_…")` 이 **칸 전체**를 간다. 줄바꿈으로 문단을 나눠 넣고
  **남는 문단은 비운다.** 줄이 문단보다 많으면 거절한다 — 말없이 합치지 않는다.
- `op:"replace"` 가 **글자 칸 하나 안에서만** 바꾼다. 걸치면 거절하고 `set_text` 를 시킨다.
  문단을 통째로 갈면 런이 합쳐져 굵기·색이 날아가기 때문이다.
- 글을 갈 때 묵은 `hp:linesegarray` 를 지운다 — 다만 **짜임(글자 수 + 폭 갈래)이
  달라졌을 때만.** 늘 지우면 한글이 다시 재면서 쪽이 늘어난다(23쪽 → 26쪽).
- 표에 줄을 넣을 때 `rowAddr` 를 다시 매기고 `rowCnt` 를 올린다.
  본뜬 줄에서 표·그림을 뺀다.
- 구역을 더할 때 `<hh:head secCnt>` 까지 올린다 — 안 올리면 한글이 새 구역을 통째로 버린다.

**안내하는 말**

- 오류 안내에서 **안쪽 함수 이름을 뺐다.** `ID매기기()` 는 부르는 쪽에 없는 것이다.
- `compose` · `create_document` 가 **「채울 양식이 열려 있으면 쓰지 마라」** 고 말한다.
  한 엔진이 양식을 놔두고 새 문서를 지어 딴 폴더에 저장한 일이 있었다.

### 성질

- **왕복 무손실.** 안 고친 XML 노드는 원본 조각을 그대로 뱉고, 안 고친 zip 부품은
  원본 압축 바이트를 그대로 쓴다. 아무것도 안 고치면 **바이트가 같다.**
- **정규식으로 XML 을 수술하지 않는다.** 그런 자리가 0곳이다.
- **저장 길목에서 규격을 본다.** 다만 **새로 낸 흠**만 막는다 —
  남의 문서에 원래 있던 흠까지 막으면 열지도 못한다.

### 재 본 것

    낱개 시험      486개
    고장 내보기    63건 — 일부러 고장 내서 시험이 실제로 깨지는지 본다
    검증 갈래      한글 없이 13 · 한글까지 22

한글로 실제로 열어 PDF 로 구워 **픽셀까지 대 본다.** 양식 16개 × 손 5가지 = 57판.

### 아직 못 하는 것

- 기능 59가지 가운데 44가지. 각주·미주·메모·수식은 **읽기만** 된다.
- 정부 문서를 맨바닥에서 재현하면 35쪽 가운데 6쪽이 넘친다.
  닮음 75%(글자 크기 57%, 선 42%). **양식 채우기는 이 수와 상관없다** —
  그쪽은 57판 무탈이다.

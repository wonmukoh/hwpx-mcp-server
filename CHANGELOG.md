# 판올림 기록

## 0.6.0 — 2026-09-03

### 새로 되는 것

- **`render_html` — 문서를 HTML 한 장으로 엮는다.** 열두 번째 도구다.

  `.hwpx` 는 브라우저가 못 연다. 그래서 이 서버를 물고 도는 앱이 문서를
  **만들 수는 있어도 보여 줄 수가 없었다** — 한글이 깔려 있어야만 눈으로 봤다.
  HTML 로 한 번 옮겨 두면 미리보기·인쇄·PDF·워드 붙여넣기가 한꺼번에 풀린다.

  그림은 `data:` 로 파일 안에 박는다. **딸린 폴더가 없다.**
  큰 문서는 `path` 를 주면 파일로 쓰고 경로만 돌려준다 — 안 주고 3MB 를
  답에 실으면 부르는 쪽 컨텍스트가 통째로 먹힌다.

  **쪽이 어디서 넘어가는지는 한글과 다르다.** 표본 9편을 한글 PDF 와 대니
  글은 99.5% 남는데 쪽수까지 같은 것은 5편이었다. 미리보기·검토용이다.

- **표 편집의 빠진 절반을 채웠다** — `edit` 에 `op` 일곱을 더한다.

      insert_col    칸(열)을 넣는다
      delete_col    칸을 뺀다 — 빈 칸만 (force 로 넘긴다)
      merge_cells   칸을 합친다
      split_cell    합침을 푼다. rows·cols 를 주면 성한 칸에 금을 긋는다
      split_table   표를 두 개로 가른다
      join_tables   표 둘을 이어 붙인다
      set_table     쪽 경계에서 나누는 방식 · 머리 줄 되풀이

  **줄은 넣고 뺄 수 있는데 칸은 아무것도 없었다.** 양식에 항목이 하나 늘면
  열을 더해야 하는데 그럴 길이 없어 표를 통째로 새로 짜야 했다.
  합치기도 안쪽에만 있고 도구로는 못 불렀고, **푸는 짝은 아예 없었다.**

  칸을 넣고 뺄 때 **표 전체 폭은 그대로 둔다.** 새 칸에 이웃 폭을 주면
  표가 넓어져 쪽 밖으로 나간다 — 양식은 폭이 정해진 것이다.

  지우는 것은 `delete_row` 와 같은 셋을 막는다 — 마지막 칸, 합친 칸을 반만
  지우는 것, **글이 든 칸**.

  **쪽 경계에서 나누는 방식**은 표 조각에 `CELL` 이 박혀 있고 바꿀 길이 없었다.
  한 쪽에 붙어 있어야 하는 결재란·서명란이 `NONE` 이라야 하는데 못 줬다.
  실측에서 셋이 다 쓰인다 — `CELL` 333 · `NONE` 20 · `TABLE` 2.

  **셀 나누기**는 HWPX 에 「이 칸만 둘로」 가 없어서, 표 전체에 금을 긋고
  나머지 줄에서는 도로 합치는 식으로 한다. 한글이 하는 것도 그것이다.
  그래서 나눈 칸의 폭만 쪼개지고 다른 열의 폭은 그대로다.

- **`compose` 가 칸마다 다르게 꾸민다** — `cells` 와 `border_width: 'none'`.

  표 하나에 테두리 하나만 걸면 **격자**가 된다. 그런데 사례집·계획서 양식은
  세로선을 걷어내고 **가로선만** 남기며, 머리 줄이나 합계 줄 한 칸에만
  배경을 깐다. 그건 칸마다 `borderFill` 이 달라야 나오는데 그 길이 없어
  격자표밖에 못 만들었다.

      cells: [
        { row: 0, background: '#F2F5F9', bottom: '0.4 mm #2A5DA8' },
        { col: 0, align: 'left' },
        { row: 2, col: 1, bold: true, color: '#C0392B' },
      ]

  `row` 만 주면 그 줄 전체, `col` 만 주면 그 열 전체, 둘 다 주면 그 칸 하나.
  **적은 차례대로 덮는다** — 넓은 것을 먼저, 좁은 것을 나중에 적는다.
  배경·네 면 선·가로세로 정렬·굵게·글자색·크기를 준다.

  같은 꾸밈이 여러 칸에 걸리므로 만든 `borderFill` 을 **되쓴다.**
  안 그러면 표 하나에 `borderFill` 이 수십 개 생긴다.

  **굵기 0 은 선을 안 지운다.** `border_width: '0 mm'` 를 줘도 격자가 그대로
  나왔다 — 한글이 가장 가는 `0.1mm` 로 올려 그린다. 지우려면 종류가 `NONE`
  이어야 한다. 그래서 `'none'` 을 따로 알아듣게 했다.

  칸 안에서 `
` 이 문단으로 갈라진다. 글자 칸(`hp:t`)에 그대로 넣으면
  한글이 줄을 안 바꾼다 — 지도안 표는 한 칸이 여러 줄이다.

  이 판을 굽기 전까지 **도구 설명에는 `cells` 가 있는데 그리는 쪽이 없었다.**
  주면 받고 조용히 버렸다. 설명에 내건 것은 다 되게 해 둔다.

### 여기서 배운 것 — 「엮었다」와 「읽을 만하다」는 다르다

HTML 은 어지간하면 나온다. 그래서 **나온 것을 PDF 로 구워 한글 것과 대 보기
전까지는 아무것도 몰랐다.** 아래는 다 그렇게 잡은 것이다.

- **문서 한 장에 통째로 취소선이 그어졌다.** 굵게가 「요소가 있으면 켜진 것」
  이라 취소선도 그럴 줄 알았다. 취소선은 `@type` 이 아니라 **`@shape`** 를 쓰고,
  안 그은 글자에도 요소가 그대로 붙어 있다.

  `shape="NONE"` 만 걸러도 모자랐다 — 표본에 **`3D` 가 833개**였고,
  교육부 업무계획은 `charPr` 125개 중 **124개가 `3D`** 였다.

- **A4 세로 문서가 가로 PDF 로 나왔다.** `landscape="WIDELY"` 를 가로로 읽었다.
  세어 보니 구역 36개 중 35개가 `WIDELY` 인데 **가로인 문서는 하나도 없다.**
  치수에 이미 방향이 들어 있다.

- **29,154쪽짜리 PDF 가 나왔다.** `hp:offset/@y` 에 `4294955572` 가 있었다 —
  2³² − 11724, 즉 **−11724** 를 부호 없는 32비트로 적어 둔 것이다.
  그대로 읽어 15km 짜리 자리를 만들었다.

- **절 제목 넷이 통째로 사라졌다.** 「제목 띠」가 배경 그림과 글상자를
  `hp:container` 로 묶은 꼴인데, 묶은 개체를 건너뛰고 있었다.
  도구는 아무 탈도 안 냈다 — 한글 PDF 와 글을 세어 보고서야 알았다.

- **띠 제목이 쪽 왼쪽 밖으로 밀려 잘렸다.** 뜬 개체를 그냥 줄 안에 넣었더니
  그 문단의 내어쓰기(−25.26mm)를 따라갔다. 「예산 집행 계획」이 **「행 계획」**
  이 되어 있었다.

- **표 제목(`hp:caption`)을 안 냈다.** 표 안이 아니라 표에 딸린 자리라
  칸만 훑으면 안 보인다.

- **칸 주소를 0,1,2… 로 매기면 줄이 왼쪽으로 밀린다.** `colAddr` 은 절대 열
  번호라, 위에서 세로로 덮인 자리는 그 줄에 셀이 **아예 없다.** 그리고
  `colCnt = 칸수` 는 제자리를 맴돈다 — `칸수` 가 `colCnt` 를 읽기 때문이다.
  칸을 지워도 수가 안 줄어 「열이 3개인데 폭을 2개 줬다」 로 걸렸다
  ([`자료/실측.md`](자료/실측.md) 25항).

- **칸 배경이 통째로 빠졌다.** `hc:solidFill`·`hc:gradFill` 이라는 그럴듯한
  이름을 찾고 있었는데 **표본 33편에 그 이름은 한 번도 안 나온다.**
  실제로 쓰는 것은 `hc:winBrush` 387 · `hc:imgBrush` 17 · `hc:gradation` 11 이다.

  **칸 959개가 배경을 잃은 채** 나갔다. 표 머리 줄의 띠 색이 빠지는데도
  글은 다 있어서 글자 수로는 안 걸렸고, `디자인닮음.py` 는 칸 배경을 안 본다.
  **갈래 스물셋이 다 파랬다.** 이제 그러데이션까지 CSS 로 옮긴다.

  자세한 것은 [`자료/실측.md`](자료/실측.md) 18~24항.

### 재는 것을 늘렸다

- **HTML 닮음 갈래.** 같은 문서를 한글로 굽고, 엮은 HTML 을 브라우저로 굽고,
  둘을 견준다. **글이 사라지는 것만** 갈래를 깨뜨린다 — 쪽수는 다를 수밖에 없다.

      글 남음 99.5% · 쪽수까지 같은 것 5/9 · 디자인 닮음 69%

  이 갈래의 잣대도 한 번 갈았다. 여덟 글자씩 토막 내어 세었더니
  목차 점선 하나가 토막을 깨뜨려 **글은 다 있는데 87%** 가 나왔다.
  잣대가 무엇을 재는지 모르면 그 수는 아무 말도 안 해 준다.

- 고장 내보기 **82 → 111건**, 낱개 시험 **543 → 615개**, 검증 갈래 **22 → 23**.

### 고친 것

- **`edit` 이 반쯤 됐을 때 몇 개가 됐는지 수로 알려 준다.**
  하나가 어긋나면 거기서 멈추되 **앞의 것은 안 물린다.** 그걸 말로만 적어 뒀더니
  부르는 쪽이 거꾸로 읽고 다시 넣어 **두 번 쓸** 뻔했다.
  이제 `done` · `failed_at` · `failed_op` · `results` 를 답에 담는다.

- **`--version` 이 어느 소스에서 구웠는지도 낸다.**

      0.5.0 (99a4107)              올린 그대로 구운 것
      0.5.0 (99a4107, 고치는 중)    안 올린 변경이 있는 채로 구운 것

  판 번호로는 번들을 못 가린다 — 개발 중에는 판을 안 올리고 소스만 고치는 게 흔해서,
  어제 것과 오늘 것이 둘 다 `0.5.0` 이라 답하면서 동작이 다르다.
  실제로 「이 번들에 그 고침이 들었나」 를 알아내려고 **도구 지문을 뜨고
  stdio 로 두드려야** 했다. 굽는 자리에 `구운것.json` 을 찍어 그 일을 없앴다.

- 검증이 **「고친 그 자리」** 를 본다. `find(text:…)` 로 문서 어딘가를 재던 것을
  `get_content(id)` 로 좁혔다 — 양식은 같은 낱말이 여러 자리에 나와서,
  딴 자리 것만 걸려도 통과했다. **옛 방식이 정말 속는지도 되돌려 재 봤다.**

- 시험 둘이 서로의 `dist` 를 갈아치우던 **간헐적** 깨짐을 없앴다.
  아무도 안 건드리는 `.빌드전체` 를 묶게 해 **구조로** 없앴다.

---

## 0.5.0 — 2026-08-28

**처음 쓰는 사람처럼 깐 꾸러미를 훑다가** 나온 것들이다.
검증 22갈래로는 하나도 안 나왔다 — 「도구가 답하나」 와 「처음 만난 사람이 쓸 수 있나」 는
다른 물음이었다.

### 새로 되는 것

- **`edit` 에 `delete_row`.** `insert_row` 는 있는데 **지우는 짝이 없었다.**
  양식에는 넉넉히 만들어 둔 줄이 있다 — 예산 표에 다섯 줄인데 쓸 것이 셋이면
  둘이 남는다. 넣을 수만 있고 뺄 수는 없으니 빈 줄을 그대로 두는 수밖에 없었다.

  지우는 것은 되돌릴 수 없어 **셋을 막는다** — 마지막 줄, 합친 칸을 반만 지우는 것,
  **글이 든 줄**. 셋째는 `force: true` 로 넘기되 지운 글은 못 되돌린다.
  `at` 은 **꼭 줘야 한다** — `insert_row` 처럼 기본값을 두면 엉뚱한 줄이 말없이 날아간다.

- **`--help` 와 `--version`.** 처음 쓰는 사람이 `npx hwpx-mcp` 다음으로 가장 먼저
  치는 것이 `--help` 인데, 인자를 통째로 무시하고 stdio 서버를 띄워
  **멈춰 선 것처럼** 보였다.

### 고친 것

- **XML 이 못 쓰는 글자를 그냥 받았다.** `U+0000` 같은 C0 제어문자는 XML 1.0 에서
  **어떤 방법으로도 못 쓴다** — 숫자 참조로도 안 된다. 그런데 넣으면 **저장까지 됐고**,
  그 파일을 한글에 물으니 `OPENFAIL` 이었다. 저장이 됐다고 쓸 수 있는 게 아니다.

  이제 쓰는 길목에서 **몇 번째 글자인지 짚어 주고** 막는다. 말없이 빼지 않는다 —
  빼면 글이 조용히 달라진다. 저장 길목에도 그물을 걸어 딴 길로 새어 들어와도 잡는다.
  줄바꿈·탭은 규격이 허락하는 것이라 그대로 통과한다.

  **한 길만 막았더니 딴 길로 샜다.** 처음엔 `set_text` 만 막았는데 `replace` 로는
  그냥 들어갔다. 글이 들어오는 길 여덟을 하나씩 찔러 보고 다 막았다 —
  `set_text`(문단·칸) · `replace` · `compose`(본문·표 칸·제목·개조식·머리말).
  `compose` 는 **어느 블록의 어느 자리**인지까지 짚는다:
  `blocks[2].rows[1][1] 의 1번째 글자 U+0000`.

- **합친 칸이 안 보였다.** 덮인 자리가 딴 칸처럼 나와서 —

      표 전체:  0,0="여기"  1,0="여기"      ← 한 칸인데 두 번 나온다
      cell_x_1_0 에 쓰면 → "이미 같은 글이라 바뀐 것이 없다"

  양식을 채우는 모델이 이걸 보면 **두 칸인 줄 알고** 서로 다른 걸 쓰려다 막힌다.
  그리고 그 문구로는 왜 막혔는지 알 길이 없다.

  이제 `get_content(표)` 가 `rowspan`·`colspan`·`covered_by` 를 낸다.
  덮인 자리에 쓰려 하면 **어느 칸을 쓰라고 짚어 준다.**
  `find(kind:"cell")` 도 덮인 자리는 안 낸다.

- **굽기가 겹쳐 돌면 `dist` 가 깨졌다.** 검증 한 바퀴가 여섯 번 굽고 vitest 는
  시험 파일을 나란히 돌린다. 자리 이름이 같아 서로의 굽는 폴더를 지웠고,
  둘이 동시에 옮기다 `EPERM` 이 났고, **바꾼 뒤에 `dist` 를 열어 확인하느라**
  손잡이를 쥔 채라 남이 이름을 못 바꿨다.
  이제 자리에 pid 를 붙이고, 확인을 **자리 바꾸기 앞**에서 하고, 바꾸는 순간만
  줄 세운다. 넷을 동시에 돌려도 오류가 없다.

---

## 0.4.1 — 2026-08-28

### 고친 것

- **서버가 제 판을 잘못 말했다.** `package.json` 은 0.4.0 인데 클라이언트에게는
  `0.1.0` 이라고 답했다 — 판이 두 군데 따로 적혀 있었다.
  이제 `package.json` 에서 읽는다. 갈라지면 시험이 걸린다.
- **`repository` 를 적었다.** 없어서 npm 페이지의 문서 링크가 다 죽어 있었다.
  파일은 꾸러미에 담겨 있었지만, npm 웹은 상대 링크를 저장소 기준으로 푼다.
- `compose` · `create_document` 설명에 **「채울 양식이 이미 열려 있으면 쓰지 마라」** 를 넣었다.
  한 엔진이 열어 둔 양식을 놔두고 새 문서를 지어 딴 폴더에 저장한 일이 있었다.

### 재는 것을 늘렸다

- **규격판 여섯 가지**로 두드려 본다 — SDK 가 받는 다섯에 **모르는 판** 하나를 더해서.
  모르는 판을 보내도 거절하지 않고 아는 최신 판으로 답하는지 잰다.
  클라이언트 이름을 세는 대신 규격으로 잰다.
- **헛도는 시험을 찾는 갈래.** 앞에 `expect` 가 없는 건너뛰기, 이 기계에만 있는 절대 경로,
  `expect` 가 하나도 없는 `it` 을 잡는다. 만들자마자 둘을 잡았다.
- **내놓을 채비 갈래.** README 가 가리키는 문서가 꾸러미에 담기나, 라이선스가 있나,
  `bin` 이 죽지 않나. `prepublishOnly` 에도 박혀 있다.

### 그 밖에

- 안티그래비티 설치 안내 (`~/.gemini/config/mcp_config.json`)
- 시험 자료에서 실제 학교 이름을 빼고 지어낸 이름으로 갈았다

---

## 0.4.0 — 2026-08-27 · 첫 배포

한글 문서(HWPX)를 **한글 없이** 읽고 쓰는 MCP 서버. 도구 열한 개.

이 판부터 **실제로 쓰인다.** 학교 문서 작성 앱이 이걸 물고 돌고,
가정통신문·계획서를 도구만으로 끝까지 만든다.

### 도구

| | |
|---|---|
| 문서 | `create_document` `open_document` `save_document` `close_document` `list_documents` |
| 읽기 | `get_outline` `get_content` `find` `get_styles` |
| 쓰기 | `compose`(맨바닥에서 짠다) `edit`(있는 것을 고친다) |

`edit` 은 `op` 로 가른다 — `set_text` · `replace` · `set_style` · `insert_row` · `insert_image`.

### 왜 열한 개인가

옛 판은 125개였다. 서버에 물어 센 수다.

| | 옛 판 | 0.4.0 |
|---|---|---|
| 도구 | 125개 | **11개** |
| `tools/list` 답 | 71,926자 | **18,211자** |

`tools/list` 는 **매 요청마다** 붙는다. 이 차이가 거기서 값을 한다.

### 이 판에서 고친 것

**읽기**

- `get_content(doc_id)` — `id` 없이 부르면 **문서 전체 글**. 고치기 전에 흐름을 읽는 길.
- `get_content(id:"tbl_…")` — 칸마다 `{id, text, row, col}` 와 `empty`.
  「셋째 줄」 같은 **위치 지시를 ID 로 옮길** 수 있다.
- `get_content(id:"cell_…")` — 칸 글을 **줄바꿈으로 갈라** 주고 `paragraphs` 로 문단 ID 까지.
- `find(kind:"paragraph")` — **문서 차례대로**, 빈 문단까지, 기본 500개.
  빈 자리는 글이 없어 `text` 로 못 찾는다. 이 길이 유일하다.
- `find` 의 문단마다 **`in_cell`** — 셀 안의 표까지 내려가 **가장 안쪽 칸**을 가리킨다.
- `find(kind:"table")` 에 `rows` · `cols` · **`empty`** — 빈 표를 가릴 수 있다.
- `find` · `get_outline` 이 **`truncated`** 를 낸다. 잘렸으면 그렇다고 말한다.

**쓰기**

- `set_text(id:"cell_…")` 이 **칸 전체**를 간다. 줄바꿈으로 문단을 나눠 넣고
  **남는 문단은 비운다.** 줄이 문단보다 많으면 거절한다 — 말없이 합치지 않는다.
- `op:"replace"` 가 **글자 칸 하나 안에서만** 바꾼다. 걸치면 거절하고 `set_text` 를 시킨다.
  문단을 통째로 갈면 런이 합쳐져 굵기·색이 날아가기 때문이다.
- 글을 갈 때 묵은 `hp:linesegarray` 를 지운다 — 다만 **짜임(글자 수 + 폭 갈래)이
  달라졌을 때만.** 늘 지우면 한글이 다시 재면서 쪽이 늘어난다(23쪽 → 26쪽).
- 표에 줄을 넣을 때 `rowAddr` 를 다시 매기고 `rowCnt` 를 올린다.
  본뜬 줄에서 표·그림을 뺀다.
- 구역을 더할 때 `<hh:head secCnt>` 까지 올린다 — 안 올리면 한글이 새 구역을 통째로 버린다.

**안내하는 말**

- 오류 안내에서 **안쪽 함수 이름을 뺐다.** `ID매기기()` 는 부르는 쪽에 없는 것이다.
- `compose` · `create_document` 가 **「채울 양식이 열려 있으면 쓰지 마라」** 고 말한다.
  한 엔진이 양식을 놔두고 새 문서를 지어 딴 폴더에 저장한 일이 있었다.

### 성질

- **왕복 무손실.** 안 고친 XML 노드는 원본 조각을 그대로 뱉고, 안 고친 zip 부품은
  원본 압축 바이트를 그대로 쓴다. 아무것도 안 고치면 **바이트가 같다.**
- **정규식으로 XML 을 수술하지 않는다.** 그런 자리가 0곳이다.
- **저장 길목에서 규격을 본다.** 다만 **새로 낸 흠**만 막는다 —
  남의 문서에 원래 있던 흠까지 막으면 열지도 못한다.

### 재 본 것

    낱개 시험      486개
    고장 내보기    63건 — 일부러 고장 내서 시험이 실제로 깨지는지 본다
    검증 갈래      한글 없이 13 · 한글까지 22

한글로 실제로 열어 PDF 로 구워 **픽셀까지 대 본다.** 양식 16개 × 손 5가지 = 57판.

### 아직 못 하는 것

- 기능 59가지 가운데 44가지. 각주·미주·메모·수식은 **읽기만** 된다.
- 정부 문서를 맨바닥에서 재현하면 35쪽 가운데 6쪽이 넘친다.
  닮음 75%(글자 크기 57%, 선 42%). **양식 채우기는 이 수와 상관없다** —
  그쪽은 57판 무탈이다.

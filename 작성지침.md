# 작성 지침

이 프로젝트에서 코드와 문서를 쓰는 방법. **다른 지침보다 이것이 우선한다.**

---

## 1. 조용히 실패하지 않는다

이 프로젝트가 존재하는 이유다. 지금 쓰는 MCP 를 감사했을 때
**"성공했습니다" 라고 답하고 파일에는 아무것도 안 남긴 도구가 21개**였다.

### 규칙

- 연산은 **성공 / 실패(이유)** 중 하나를 돌려준다. 그 중간은 없다.
- "아마 됐을 것" 을 성공으로 치지 않는다. 확인할 수 없으면 실패다.
- 못 하는 일은 **못 한다고 말한다.** 조용히 넘어가는 것보다 낫다.
- 안 되는 이유는 **다음에 무엇을 하면 되는지**까지 적는다.

```ts
// 이렇게
return fail('표 3번을 찾지 못했습니다. 이 문서에는 표가 2개 있습니다(0, 1).');

// 이렇게 하지 않는다
return ok();          // 실은 아무 일도 안 했다
if (!table) return;   // 부르는 쪽은 성공한 줄 안다
```

### 조용한 실패가 숨는 자리 — 실제로 물린 것들

| 모양 | 무슨 일이 벌어지나 |
|---|---|
| `?? 0` / `\|\| 기본값` | 못 찾았는데 0번을 쓴다. 글꼴이 안 바뀌는데 성공이라고 답했다 |
| `if (args.x)` | `0` 과 `''` 을 '안 준 것' 으로 친다. 여백 0 을 못 준다 |
| `catch { }` | 삼킨다 |
| `if (!found) return xml;` | 원본을 그대로 돌려주고 부르는 쪽은 바뀐 줄 안다 |
| 범위 밖 번호 | `sections[undefined]`, `elements[999]` — 조용히 넘어간다 |

**새 코드에 이 모양이 보이면 그 자리에서 고친다.**

---

## 2. XML 을 문자열로 수술하지 않는다

지금 MCP 버그의 절반이 여기서 나왔다.

- `<hp:p>` 를 찾는 정규식이 `<hp:pic>` 을 문다
- `<hp:footNote>` 검사가 `<hp:footNotePr>` 에 걸린다
- 문단 끝을 '첫 번째 `</hp:p>`' 로 잡으면 각주 안의 문단이 걸린다

### 규칙

- **파싱해서 트리로 다룬다.** 읽기 → 모델 → 쓰기.
- 정규식으로 XML 을 고치는 코드는 **리뷰에서 막는다.**
  (읽기 전용 검사·조회는 괜찮다. 고치는 것이 문제다)
- 모르는 요소·속성은 **원형 그대로 보존한다.** 남의 문서를 열어 한 글자만
  고치고 저장했을 때 나머지가 바뀌면 안 된다.

---

## 3. 한글이 어떻게 하는지 추측하지 않는다

형식 문서만 봐서는 모른다. 문서에 안 적힌 것이 많고, 적힌 대로 해도 한글이 안 읽는
경우가 있다.

### 규칙

- 새 기능을 만들기 전에 **한글 2024 로 그 기능을 쓴 파일을 만든다** (COM 자동화).
  `자료/기준파일/` 에 넣는다.
- 우리가 만든 XML 과 **바이트 단위로 비교**한다.
- 기준 파일이 없으면 그 기능은 시작하지 않는다.

이렇게 알아낸 것들 (전부 문서에 없거나 다르게 적혀 있었다):

- 한글은 **압축된 BinData 를 읽지 않는다.** 그림을 STORE 로 넣어야 보인다.
- 머리말·꼬리말·각주·쪽번호는 전부 **문단 안 `<hp:ctrl>` 컨트롤**이다. secPr 자식이 아니다.
- `</hp:secPr>` 다음은 **`colPr` 자리**다. 그 앞에 다른 컨트롤을 끼우면 구역 속성이 통째로 죽는다.
- 한글은 파일을 열 때 **표 높이를 다시 계산하지 않는다.** 만드는 쪽이 넣어야 글자가 안 잘린다.
- 각주 본문에 `<hp:autoNum>` 이 없으면 각주를 통째로 버린다.

---

## 4. 눈으로 본 것만 됐다고 한다

XML 검사를 통과해도 한글이 안 그리는 경우가 **반복해서** 나왔다.

### 규칙

- 개체(그림·표·각주·머리말·쪽 테두리)를 건드렸으면 **PDF 로 뽑아 본다.**
- 사람이 볼 필요 없이 기계로 재는 것을 우선한다 — pdfplumber 로 선·글자 위치를 센다.
- 검증 기준이 틀릴 수도 있다는 것을 잊지 않는다.
  (PDF 에 링크 주석이 없다고 링크가 아닌 게 아니었다. 한글이 PDF 로 링크를 안 내보낸다)

---

## 5. 부르는 쪽이 세지 않게 한다

지금 MCP 는 `after_index` / `paragraph_index` 로 위치를 가리킨다.
표지 제목은 요소를 3개 늘리고 개조식은 줄 수만큼 늘린다. **부르는 쪽이 셀 수 없다.**
문서를 만들면서 내가 직접 세 번 틀렸다.

### 규칙

- 요소를 가리킬 땐 **안정 ID** 를 쓴다. 번호는 조회용이지 지시용이 아니다.
- 무언가를 만든 도구는 **만든 것의 ID 를 돌려준다.**
- 문서를 새로 쓸 때는 **선언적 조판**(블록 목록)이 기본 경로다.

---

## 6. 이름을 한 번만 정한다

지금 MCP 의 행·열 도구는 이렇다.

```
insert_table_row     after_row
insert_table_column  after_col
delete_table_row     row_index
delete_table_column  col_index
```

같은 뜻인데 이름이 넷이다. 오늘 이걸로 세 번 틀렸다.

### 규칙

- 도구 이름은 `동사_명사` 소문자·밑줄. 예: `insert_row`, `delete_row`
- 같은 뜻에는 **같은 이름**. `row` / `col` / `index` / `id` 를 한 번만 정하고 끝까지 쓴다.
- 매개변수 이름 표는 [`기획/04-도구-설계.md`](기획/04-도구-설계.md) 에 두고, 거기 없는 이름은 안 만든다.

---

## 7. 도구 설명은 모델이 읽는 글이다

사람 문서가 아니다. 모델이 이걸 읽고 고른다.

- 첫 문장에 **무엇을 하는지**. 그 다음에 **언제 쓰는지**.
- 비슷한 도구가 있으면 **구분점**을 적는다.
  ("문단 전체를 바꾸려면 `set_text_style`, 문장 일부만 바꾸려면 `emphasize_text`")
- 단위를 적는다 (pt / mm / HWPUNIT).
- 예시를 한 줄 넣는다.

---

## 8. 한국어로 쓴다

- 주석·문서·오류 문구는 한국어. 코드 식별자는 영어.
- **무엇을 하는지가 아니라 왜 그렇게 했는지**를 적는다.
  코드를 읽으면 무엇을 하는지는 보인다. 왜 그런 이상한 짓을 하는지는 안 보인다.

```ts
// 이렇게
// 한글은 압축된 BinData 를 읽지 않는다. 파일 안에 있어도 화면에 안 나온다.
zip.file(path, buf, { compression: 'STORE' });

// 이렇게 하지 않는다
// BinData 를 STORE 로 저장한다
zip.file(path, buf, { compression: 'STORE' });
```

- 형식 문서 용어는 한글 표기를 쓴다: 문단(paragraph), 글자 모양(charPr),
  문단 모양(paraPr), 테두리·배경(borderFill), 구역(section), 개조식, 내어쓰기.

---

## 9. 검사식도 의심한다

각주 검사가 `secPr` 의 **설정** 태그에 걸려 **늘 통과**하고 있었다.
코드에서 열 번 고친 함정을 감사 스크립트에서 또 밟았다.

2단계에서 두 번 더 물렸다.

**하나 — 검사가 지나가지 않는 길이 있었다.**
왕복 검사는 *안 고친* 문서만 봤다. 안 고치면 원본 조각을 그대로 뱉으니
다시 쓰는 길은 한 번도 밟히지 않았다. 그 틈으로 `name=""` 을 `name` 으로 쓰는
버그가 빠져나가 한글이 파일을 거부했다.
802개 부품 가운데 **349개**가 그 꼴이었는데 검사는 전부 통과라고 했다.

**둘 — 검사가 0편을 보고 통과라고 했다.**
목록 파일이 상대 경로여서 113편이 전부 조용히 걸러졌는데 훑기는 `실패 0건` 이라고 찍었다.

### 규칙

- 검사를 새로 쓰면 **일부러 고장 내서** 그 검사가 실패하는지 본다.
- 통과하는 검사보다 **실패해야 할 때 실패하는 검사**가 값어치가 있다.
- 무엇을 '통과' 로 볼지 자체를 적어 둔다.
- **검사가 지나가지 않는 길이 있는지 본다.** 빠른 길(원본 그대로 뱉기)이 있으면
  느린 길(손으로 다시 쓰기)을 **일부러 밟게 하는 갈래**를 따로 낸다.
- **검사는 자기가 무엇을 봤는지 숫자로 말한다.** 몇 편, 몇 부품, 몇 개에 표시를 심었는지.
  숫자가 0이면 통과가 아니라 실패다.

---

## 10. 작은 것부터, 매번 돌려 본다

- 계층 하나가 끝나면 그 계층만으로 돌아가는 시험을 만든다.
- 규격 문서를 다 읽고 시작하지 않는다. **기준 파일 한 개**로 시작한다.
- 한 번에 한 가지만 바꾸고 회귀를 돌린다. 두 가지를 같이 바꾸면 어느 쪽인지 모른다.

---

## 도구 쓸 때 주의 (이 저장소에서 작업할 때)

- **역슬래시.** 실측: 따옴표 heredoc 안에서 홑 역슬래시는 살아남지만
  겹친 것은 하나로 줄어든다. `node -e "..."` 는 셸 따옴표를 한 겹 더 먹으니 더 위험하다.
  → 규칙: 패치 스크립트는 **파일로 써서 돌린다.** 역슬래시가 필요하면
  `String.fromCharCode(92)` 로 만든다. (이 규칙을 쓰다가 이 규칙에 물렸다 —
  `\\b` 라고 쓴 것이 `\b`(백스페이스 0x08)로 들어갔다.)
  의심되면 `od -c` 로 바이트를 본다.
- **PowerShell 5.1 은 .ps1 을 ANSI 로 읽는다.** 한글 주석이 있으면 UTF-8 **BOM** 으로 저장.
  주석만이 아니다 — **경로에 한글이 있으면 경로가 깨진다.**
  `Temp/hwpx-단위/여백2000.hwpx` 가 `Temp/hwpx-?ⓒ쐘?ㅼ갏2000.hwpx` 가 되어
  없는 폴더에 저장하려다 한글이 오류 대화상자를 띄웠다.
  → 규칙: **.ps1 파일에는 한글을 넣지 않는다.** 한글이 든 경로는 `-Command` 로 넘긴다.
- **한글 보안 대화상자는 OneDrive 경로 때문에 뜬다.** 로컬 임시 폴더에서 열면 안 뜬다.
- **PowerShell 의 백틱은 JS 템플릿 문자열을 끊는다.** PowerShell 조각을 JS 에서 만들 땐
  백틱 이스케이프를 쓰지 말고 평범한 구분자(` ||| `)를 쓴다.
- **바꿔치기는 맞았는지 확인한다.** `String.replace` 는 못 찾으면 조용히 원본을 돌려준다.
  패치 스크립트에서 이걸 안 하면 "고쳤다"고 말하고 아무것도 안 고친다. (1번 규칙이 여기도 온다)
- **Windows 스토어 `python` 은 껍데기다.** bash 에서 부르면 종료 코드 49 를 뱉고 아무것도 안 한다.
  파일 손질은 `node` 로 한다.
- **소스가 CRLF 다.** 찾을 글도 CRLF 로 맞춘다. 안 그러면 못 찾는다.
- Git 이 한글 파일명을 이스케이프한다. `git config core.quotepath false`.
